<% provide(:title, 'ユーザー一覧') %>

<h1>登録ユーザー一覧</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= user.name %> id: <%= user.id %> 登録企業数： <%= user.corporates.count %>
    </li>
  <% end %>
</ul>