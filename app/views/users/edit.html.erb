<% provide(:title, "ユーザー設定") %>
<% provide(:button_text, "変更を保存する") %>
<h1>ユーザー設定</h1>
<h3>※パスワードを変更しない場合はパスワード欄を入力しないでください。</h3>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
    <%= link_to "アカウントを削除する", @user, method: :delete,
                      data: { confirm: "本当に削除しますか?" } %>
  </div>
</div>